#include "ngx_http_prefetch_filter_handler.h"


/*
 * 
 *
 *
 */

void
ngx_http_prefetch_handle_url(char *url)
{
	// first we need to create a nd upstream
	//ngx_http_create_nd_upstream();	
	// then we need to init the nd upstream
	
}
